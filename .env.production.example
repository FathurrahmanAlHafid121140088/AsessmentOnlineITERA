# ============================================================================
# Environment Configuration - PRODUCTION
# ============================================================================
#
# CARA PAKAI:
# 1. Copy file ini ke .env
#    cp .env.production .env
#
# 2. Edit nilai-nilai yang perlu disesuaikan (ditandai dengan <<<)
#
# 3. Generate APP_KEY:
#    php artisan key:generate
#
# 4. JANGAN COMMIT FILE .env KE GIT!
#
# ============================================================================

# ============================================================================
# Application Settings
# ============================================================================

APP_NAME="Assessment Online"
APP_ENV=production
APP_KEY=                                    # <<< Auto-generate: php artisan key:generate
APP_DEBUG=false                             # IMPORTANT: false untuk production!
APP_URL=http://assessment.kampus.ac.id     # <<< Ganti dengan domain Anda
APP_TIMEZONE=Asia/Jakarta

# ============================================================================
# Logging
# ============================================================================

LOG_CHANNEL=daily                           # daily = rotate logs otomatis
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error                             # Log only errors (bukan debug/info)

# ============================================================================
# Database Configuration
# ============================================================================

# Option 1: SQLite (Recommended untuk start, simple & cukup untuk < 10k users)
DB_CONNECTION=sqlite
DB_DATABASE=/var/www/AsessmentOnline/database/database.sqlite  # <<< Sesuaikan path

# Option 2: MySQL (Uncomment jika pakai MySQL)
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=assessment_db                  # <<< Ganti dengan nama database
# DB_USERNAME=assessment_user                # <<< Ganti dengan user database
# DB_PASSWORD=your_secure_password           # <<< Ganti dengan password database

# ============================================================================
# Cache & Session
# ============================================================================

# Database driver (reliable untuk small-medium apps)
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

CACHE_DRIVER=database
CACHE_PREFIX=

# ============================================================================
# Redis (Optional - uncomment jika pakai Redis untuk performance)
# ============================================================================

# REDIS_CLIENT=phpredis
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ============================================================================
# Mail Configuration
# ============================================================================

# Option 1: Log driver (development/testing - tidak kirim email beneran)
MAIL_MAILER=log

# Option 2: SMTP (Production - uncomment & configure jika mau kirim email)
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@kampus.ac.id     # <<< Email Anda
# MAIL_PASSWORD=your-app-password           # <<< App password (bukan password email)
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=noreply@kampus.ac.id
# MAIL_FROM_NAME="${APP_NAME}"

# ============================================================================
# Google OAuth (untuk Login dengan Google)
# ============================================================================

# Setup di: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=                           # <<< Client ID dari Google Console
GOOGLE_CLIENT_SECRET=                       # <<< Client Secret dari Google Console
GOOGLE_REDIRECT_URI="${APP_URL}/auth/google/callback"

# ============================================================================
# AWS (jika pakai S3 untuk file storage)
# ============================================================================

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-southeast-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ============================================================================
# Pusher (jika pakai real-time features)
# ============================================================================

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# ============================================================================
# Vite Configuration
# ============================================================================

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# ============================================================================
# Security Settings (Production Only)
# ============================================================================

# Uncomment jika sudah pakai HTTPS
# SESSION_SECURE_COOKIE=true
# SANCTUM_STATEFUL_DOMAINS=assessment.kampus.ac.id

# ============================================================================
# Custom Application Settings (jika ada)
# ============================================================================

# Tambahkan custom env variables di sini jika ada
# Contoh:
# MAX_KUESIONER_PER_USER=5
# ENABLE_EMAIL_NOTIFICATIONS=true
